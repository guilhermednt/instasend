DB_PATH=/data/shares.db
INTERNAL_NETWORK=10.0.0.0/24

# IP of your reverse proxy (nginx, Cloudflare tunnel, etc.)
# X-Forwarded-For headers are only trusted from this address.
FORWARDED_ALLOW_IPS=127.0.0.1
